const popupContent = {
    textAlign: "center",
    marginTop: "10px"
};
const popupHead = {
    fontWeight: "bold",
    fontSize: "22px",
    marginTop: "20px"
};

const popupText = {
    fontSize: "15px",
};

const restaddr = {
    fontSize: "14px",
    marginTop: "15px"

};

const restText = {
    fontSize: "14px",
    marginTop: "5px",
    color:"forestgreen"
}

const Heading = {
    fontFamily: "Helvetica",
    fontSize: "26px",
    marginTop: "10px",
    textAlign: "center",
    color: "grey"
}

const markOnMap = {
    textAlign: "center",
    marginTop: "10px",
    fontFamily: "Helvetica",
    fontSize: "20px"
}

const eatButton = {
    display: "flex",
    flex: "column",
    fontFamily:"Helvetica",
    fontSize:"20px",
    textAlign:"center"
}

export {
    popupContent, popupHead, popupText, restaddr, restText,
    Heading, markOnMap, eatButton
};
